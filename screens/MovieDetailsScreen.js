import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
} from 'react-native';

export default function MovieDetailsScreen() {
  return (
    <View>
      <Text>ToDo: Movie Details</Text>
    </View>
  );
}

MovieDetailsScreen.navigationOptions = {
};

const styles = StyleSheet.create({
});
